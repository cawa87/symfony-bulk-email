{% extends 'base.html.twig' %}

{% block title %}EmailList{% endblock %}

{% block body %}
    <h1>EmailList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ email_list.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ email_list.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('email_list_index') }}">back to list</a>

    <a href="{{ path('email_list_edit', {'id': email_list.id}) }}">edit</a>

    {{ include('email_list/_delete_form.html.twig') }}
{% endblock %}
